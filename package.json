{
  "name": "student.codes",
  "packageManager": "pnpm@10.18.3",
  "workspaces": ["apps/*", "bots/*", "extensions/*", "packages/*"],
  "scripts": {
    "lint": "biome check . --write",
    "format": "biome check . --write",
    "check": "turbo run check",
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=\"./packages/*\"",
    "dev": "pnpm run \"/dev:.*/\"",
    "dev:packages": "pnpm --parallel --stream --filter @evaluate/components --filter @evaluate/hooks --filter @evaluate/logger --filter piston.ts --filter virtual-file-explorer-backend run build --watch",
    "dev:apps": "turbo run dev --filter=\"./apps/*\"",
    "dev:website": "node packages/scripts/bin/dev-website",
    "prepare": "husky & ts-patch install",
    "graph": "pnpx nx@18 graph"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "@evaluate/scripts": "workspace:^",
    "@types/node": "^22.18.11",
    "husky": "^9.1.7",
    "ts-patch": "^3.3.0",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3",
    "typescript-transform-paths": "^3.5.5"
  },
  "patchedDependencies": {
    "@tailwindcss/vite@4.1.11@4.1.11": "patches/@tailwindcss__vite@4.1.11.patch"
  }
}
